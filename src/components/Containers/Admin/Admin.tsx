import React from 'react';
import { connect } from 'react-redux';

const Admin = () => {
  return <section className="authentication-section"></section>;
};

export default connect(null, null)(Admin);

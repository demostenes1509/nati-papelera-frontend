import React from 'react';
import { connect } from 'react-redux';

const Configuration = () => {
  return (
    <section className="main-forms-section">
      <p>Hello, World</p>
    </section>
  );
};

export default connect(null, null)(Configuration);
